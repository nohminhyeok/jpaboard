<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>비밀번호 수정</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
{{>layouts/header}}
  <div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="card shadow p-4" style="max-width: 400px; width: 100%;">
      <h3 class="text-center mb-4">비밀번호 수정</h3>

      <form method="post" action="/member/modifyMemberPw">
        <div class="mb-3">
          <label for="newPw" class="form-label">새 비밀번호</label>
          <input type="password" id="newPw" name="newPw" class="form-control" required>
        </div>
        <button type="submit" class="btn btn-primary w-100">비밀번호 변경</button>
      </form>
      
      <div class="text-center mt-3">
        <a href="/member/memberList" class="text-muted">← 돌아가기</a>
      </div>
    </div>
  </div>
{{>layouts/footer}}
</body>
</html>