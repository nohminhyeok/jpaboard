<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
  <title>회원 탈퇴</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8d7da;
    }
    .card {
      background-color: #ffffff;
      border-left: 5px solid #dc3545;
    }
  </style>
</head>
<body class="d-flex flex-column min-vh-100">

  <!-- Header -->
  <header class="mb-4">
    {{>layouts/header}}
  </header>

  <!-- Main Content -->
  <main class="flex-grow-1 d-flex justify-content-center align-items-center">
    <div class="card shadow p-4 text-center" style="max-width: 400px; width: 100%;">
      <h3 class="text-danger mb-3">⚠ 정말 탈퇴하시겠습니까?</h3>
      <p class="text-muted mb-4">
        탈퇴 시 모든 정보가 삭제됩니다.<br>
        이 작업은 되돌릴 수 없습니다.
      </p>

      <form method="post" action="/member/deleteMember">
        <input type="password" name="memberPw" placeholder="비밀번호 입력" required class="form-control mb-3">
        <button type="submit" class="btn btn-danger w-100">네, 탈퇴하겠습니다</button>
      </form>

      <div class="mt-3">
        <a href="/member/memberList" class="text-secondary">돌아가기</a>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="mt-auto">
    {{>layouts/footer}}
  </footer>

</body>
</html>