<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>게시판 목록</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body>
    <!-- 헤더 -->
    <div>
        <!-- 헤더 내용 -->
    </div>

    <!-- 메시지 출력 -->
    <div>
        {{msg}}
    </div>

    <!-- 게시판 목록 테이블 -->
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Title</th>
                <th scope="col">Content</th>
            </tr>
        </thead>
        <tbody>
            {{#list}}
            <tr>
                <th>{{board_no}}</th>
                <td><a href="/board/boardOne?id={{board_no}}">{{board_title}}</a></td>
                <td>{{board_content}}</td>
            </tr>
            {{/list}}
        </tbody>
    </table>

    <!-- 검색 폼 -->
    <form method="get" action="/board/boardList" class="mb-3">
        <input type="text" name="word" class="form-control" placeholder="검색어 입력">
        <button type="submit" class="btn btn-primary mt-2">검색</button>
    </form>

    <!-- 페이지네이션 -->
    <div>
        <!-- 첫 페이지가 아닐 때 -->
        {{^list.isFirst}}
        <a href="/board/boardList?currentPage={{prePage}}&word={{word}}">이전페이지</a>
        {{/list.isFirst}}

        <!-- 현재 페이지 표시 -->
        {{list.getNumber}}

        <!-- 다음 페이지가 있을 때 -->
        {{#list.hasNext}}
        <a href="/board/boardList?currentPage={{nextPage}}&word={{word}}">다음페이지</a>
        {{/list.hasNext}}
    </div>

    <!-- 새 게시글 작성 링크 -->
    <a href="/board/addBoard">새 게시글 작성</a>

    <!-- 푸터 -->
    <div>
        <!-- 푸터 내용 -->
    </div>
</body>
</html>
